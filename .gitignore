# rectal-cancer/ 는 별도의 독립 git 저장소(github.com/KANG-SURGERY/rectal-cancer)이다.
# 이 hello-world 저장소가 임베디드 저장소(gitlink)로 잘못 추적하지 않도록 제외한다.
rectal-cancer/

# OS
.DS_Store

# health-policy-council
# 회차 산출물(sessions/*/report.md, brief.md)은 추적한다 — 그게 이 시스템의 기록이다.
# 드라이런 프롬프트는 실행할 때마다 다시 만들어지므로 제외한다.
health-policy-council/sessions/*/dry-run/
.venv/
__pycache__/
*.pyc
